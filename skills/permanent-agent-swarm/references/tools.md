# Tools — permanent-agent-swarm

## Runtime
- `python main.py` / `python main.py --roster`
- `agents.permanent_runtime.build_permanent_swarm()`
- vault/audit.jsonl · claims via MemoryVault

## Grok environment
| Tool | Use |
|------|-----|
| web_search | Researcher / LiveWebScout (public) |
| google_drive_* | dual-persist skill/docs |
| github___* / gh | push swarm code |
| run_terminal_command | local swarm CLI |
| imagine_* | optional visual ops board |

## OpenCode
- agents markdown in opencode/agents/
- /models for per-agent model routing
- permissions: edit deny on Overseer/Reviewer; skill allow

## Do not
- Store secrets in swarm repo
- Bypass Supervisor
- Auto-apply Teacher skill proposals without HITL
