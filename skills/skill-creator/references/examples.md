# Skill Creator — Examples

## Basic 1 — Expand user stub
User pastes /skill-creator stub "distill drive".
→ Produce drive-skill-distiller full package (already shipped) + expand this encyclopedia.

## Basic 2 — One-job skill
Intent: "East 66 ACRIS skill"
→ east66-acris-vault with workflows/tools/examples/deployments.

## Advanced 1 — Full Drive encyclopedia
Inventory → classify → distill → 14+ skills → ENCYCLOPEDIA_INDEX → dual-persist Drive+GitHub.

## Advanced 2 — Port ImageGen registry skill
Map built-in vs CLI modes to imagine_* tools; save-path policy; prompt schema.

## Failure recovery
Triggers weak → rewrite description with exact user phrases; cold-read test.
